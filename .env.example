# Copy to .env (or set these in your host's dashboard). All optional.
PORT=3000
# Used only the first time the server starts, to create the admin account.
ADMIN_PASSWORD=ChangeMe123!
# If empty, a random secret is generated and saved in data/secret.json
JWT_SECRET=
